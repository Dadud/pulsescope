# Recorded-IQ end-to-end decoder fixtures

Synthetic clean-room IQ and audio artifacts for decoder conformance. Each entry in `manifest.json` records SHA-256, expected normalized event fields, and provenance. Fixtures are generated by `decoder_fixtures::write_canonical_fixtures` and verified in `decoder_fixtures` integration tests.

License: MIT, matching the repository.
